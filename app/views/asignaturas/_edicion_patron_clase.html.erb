<%
	unless defined? patron
		patron = PatronClase.new() 
		patron.duracion = 1
		patron.orden = 99
		patron.clase = Clase.find(@clase)
	end
%>
<%= content_tag_for(:li, patron, class: "list-group-item", data: {duracion:patron.duracion, orden: patron.orden, clase: patron.clase.id}) do %>
	<%= patron.clase.nombre %>
	<span class="badge"><input type="number" value="<%= patron.duracion %>" min="1" max="999"></input> Día(s)</span>
	<a href="#" class="eliminar glyphicon glyphicon-trash" title="Eliminar"></a>
<% end %>